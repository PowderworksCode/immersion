{
  "name": "immersion-shims",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build-shims.ts",
    "check": "tsc --noEmit",
    "build:diffs": "bun run scripts/build-diffs.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@pierre/diffs": "^1.3.5"
  }
}
